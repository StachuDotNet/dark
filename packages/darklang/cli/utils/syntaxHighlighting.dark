module Darklang.Cli.SyntaxHighlighting


/// Maps semantic token types to CLI colors.
let tokenTypeToCliColor (tokenType: LanguageTools.SemanticTokens.TokenType) : String =
  match tokenType with
  | Symbol -> Colors.blue
  | Keyword -> Colors.magenta

  | ModuleName -> Colors.brightBlack
  | TypeName -> Colors.brightBlue

  | Operator -> Colors.brightMagenta
  | String -> Colors.stringColor
  | Number -> Colors.numberColor
  | ParameterName -> Colors.brightBlue
  | VariableName -> Colors.white
  | FunctionName -> Colors.brightBlue

  | Comment -> Colors.commentColor

  | TypeParameter -> Colors.magenta
  | EnumMember -> Colors.magenta
  | Property -> Colors.white
  | Method -> Colors.blue
  | RegularExpression -> Colors.stringColor


/// Highlights one line by inserting color codes at token boundaries.
let highlightLine (line: String) (lineTokens: List<LanguageTools.SemanticTokens.SemanticToken>) : String =
  let insertions =
    lineTokens
    |> Stdlib.List.fold [] (fun acc token ->
        let startPos = token.range.start.column
        let endPos = token.range.end_.column
        let color = tokenTypeToCliColor token.tokenType

        let colorStart = (startPos, color)
        let colorEnd = (endPos, Colors.reset)

        Stdlib.List.append acc [colorStart; colorEnd])
  |> Stdlib.List.sortBy (fun (pos, _) -> pos)

  // Insert from right to left so earlier insertions do not shift later positions.
  let reversedInsertions = Stdlib.List.reverse insertions

  reversedInsertions
  |> Stdlib.List.fold line (fun currentLine (pos, colorCode) ->
      if pos >= 0 && pos <= Stdlib.String.length currentLine then
        let before = Stdlib.String.slice currentLine 0 pos
        let after = Stdlib.String.dropFirst currentLine pos
        before ++ colorCode ++ after
      else
        currentLine)


/// Applies semantic-token colors to source code.
let applyTokenColors
  (sourceCode: String)
  (tokens: List<LanguageTools.SemanticTokens.SemanticToken>)
  : String =
  let sortedTokens =
    tokens
    |> Stdlib.List.sortBy (fun token -> (token.range.start.row, token.range.start.column))

  let lines = Stdlib.String.split sourceCode "\n"

  lines
  |> Stdlib.List.indexedMap (fun lineIndex line ->
    let lineTokens =
      sortedTokens
      |> Stdlib.List.filter (fun token -> token.range.start.row == lineIndex)

    if Stdlib.List.isEmpty lineTokens then
      line
    else
      highlightLine line lineTokens)
  |> Stdlib.String.join "\n"


/// Maps a lexical token kind (from LanguageTools.Parser.lexToTokens) to a
/// semantic token type. Lexical highlighting can't tell e.g. a type name from
/// a variable name, so all identifiers map to VariableName.
let lexicalKindToTokenType
  (kind: String)
  : LanguageTools.SemanticTokens.TokenType =
  match kind with
  | "keyword" -> LanguageTools.SemanticTokens.TokenType.Keyword
  | "operator" -> LanguageTools.SemanticTokens.TokenType.Operator
  | "string" -> LanguageTools.SemanticTokens.TokenType.String
  | "number" -> LanguageTools.SemanticTokens.TokenType.Number
  | "comment" -> LanguageTools.SemanticTokens.TokenType.Comment
  | "symbol" -> LanguageTools.SemanticTokens.TokenType.Symbol
  | _ -> LanguageTools.SemanticTokens.TokenType.VariableName


/// Lexical syntax highlighting driven by the tokenizer alone.
/// Less rich than `highlightCode` (no module/type/function/parameter
/// distinction), but needs no full parse.
let highlightCodeLexical (sourceCode: String) : String =
  let nodes = LanguageTools.Parser.lexToTokens sourceCode

  let tokens =
    nodes
    |> Stdlib.List.map (fun node ->
      LanguageTools.SemanticTokens.makeToken
        node.range
        (lexicalKindToTokenType node.typ))

  applyTokenColors sourceCode tokens


let highlightCode (sourceCode: String) : String =
  // The parser is best-effort and its lexer recovers from malformed input, so it
  // always returns `Some` (a partial tree, with precise diagnostics reported
  // separately). `None` is unreachable.
  match Builtin.parserParseToWrittenTypes sourceCode with
  | Some parsedFile ->
    applyTokenColors sourceCode (LanguageTools.SemanticTokens.ParsedFile.tokenize parsedFile)
  | None -> sourceCode