module Darklang =
  module Cli =
    let init(): (State * List<Msg>) =
      let state =
        State
          { currentPage = Page.Root
            mainPrompt = ""
            thingToPrint = "" }

      // todo: maybe message to fetch stuff?
      // would it make sense to have a 'loading stuff...' screen for a sec.?
      (state, [])
