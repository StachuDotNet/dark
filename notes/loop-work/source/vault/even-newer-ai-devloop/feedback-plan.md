# Feedback plan — round 2 (2026-05-05 late morning)

The user came back from reading the docs and gave 24 substantive feedback items. This file is the prioritized checklist the loop iterates through. Each `[ ]` is roughly one iteration's worth of focused work — sometimes more for the bigger restructures.

Iteration ordering: P0 first (anything that affects tonight's 21:00 kickoff), then P1 (readability + process), then P2 (deeper rethinks). Within a tier, the loop picks the next-most-mechanical item.

---

## P0 — tonight's run shape (do these first)

The user's stated goal: kick off a process at ~21:00 tonight, wake at ~07:00 to **real numbers** comparing Dark vs other-languages on ~10 projects.

- [x] **Restructure the cross-language scope** *(iter 81)*: plan.md §4.3 now lists 5 languages (Dark/TS/Py/Go/Rust); §4.3.1 framework-pinning extended with `golang:1.23-alpine` + `rust:1.83-slim-bookworm` runtime pins; spec frontmatter `languages:` field updated globally to `[dark, ts, py, go, rust]`.
- [x] **Re-cadence the runs** *(iter 81)*: §4.3 reframed — cross-language baseline runs **weekly, manually triggered** (user kicks off; not cron); Dark runs **every day or two, manually triggered** when an improvement ships. Numbers averaged over multiple runs to smooth temperature-0-residual non-determinism. Tonight is the *kickoff*, not a recurring cron firing.
- [x] **Drop the early cost cap, pace by concurrency** *(iter 85)*: §4.9 added "Tonight's-kickoff override" — no cost cap for tonight; pace by `-j nproc/2` concurrency only. Wrapper computes per-project/per-language median costs from tonight's data; subsequent runs use 2× those medians as a *soft* cap. Hard cap only on explicit `--cap-mode hard $X`. Concurrency × wall-time naturally bounds cost for a calibration kickoff.
- [x] **Build a project queue mechanism** *(iter 89)*: created `queue-mechanism.md`. 8-phase lifecycle (queued → planning → implementing → verifying → reflecting → extracting → deleting → done), per-sweep `queue-state.json` for resumability, per-sweep `.lock` for concurrency safety, scheduler watch-loop at concurrency=4 (no cost cap per P0 #3), CLI surface (sweep / sweep --resume / status / cancel / reflect / report / dashboard). Maps cleanly onto Multi's existing queue. Wrapper crash + machine reboot both resumable.
- [x] **No `dark/` workspace dir in artifacts** *(iter 84)*: §4.0 file layout updated — no `gold/dark/` subdir committed; Dark gold = a tagged git branch `bench-gold-<project>`, ephemeral. §4.8 TS/Py/Go/Rust subdirs renamed to `artifact-<lang>/`, **purged before next attempt without peeking**. §4.10 launch checklist updated: gold references are optional for tonight; agent comparisons across languages don't require pinned gold.
- [x] **HTTP + UserDB-bound projects must be in tonight's set** *(iter 83)*: created `blockers.md` cataloguing 4 real blockers (dark-serve non-TTY flakiness — 🔴; `multi set-fork` precondition — 🟡; pricing.json — 🟡; claude OAuth — 🟡) + 2 soft concerns + 6-step pre-launch checklist. HTTP covered by `http-healthz` (#9 in queue), UserDB covered by `url-shortener-cli` (#6).
- [x] **Pick the 10 priority projects for tonight** *(iter 83)*: created `tonights-queue.md` with the 10 in priority order. 5 core projects + url-shortener-cli + csv-to-json + mvu-runtime + http-healthz + parallel-downloader. Tier spread 1 T / 6 S / 3 M. Class spread 7 apps / 2 library-ports / 1 expected-fail. Outcome spread 8 pass / 1 stretch / 1 fail-likely. 50 total attempts (10 × 5 langs); ~2h wall; ~$15 API-equivalent cost.
- [x] **Add interactive-CLI-app project class** *(iter 88)*: new `class: interactive-cli` distinct from `tui` (line-mode prompts vs full-screen). Spec format extended; new "Keystroke script" body section for rubrics. Testing pattern: `tmux send-keys` + `tmux capture-pane` + on-disk-state check. 4 candidates listed (`git-commit-wizard`, `init-project`, `ssh-config-add`, `env-doctor`); not yet materialized. Frontmatter `class:` enum now has 7 values.
- [x] **Expected-fail cut-off** *(iter 87)*: §4.9 "Cut-off policy" subsection added. Per-attempt threshold = whichever-fires-first of (50-turn budget) or (15-min wall). On abandonment: outcome=`cutoff-fail`, captures partial SUMMARY.md + last 100 lines of transcript. **3-strikes rule**: a project that hits cut-off in 3 consecutive sweeps with the same reason auto-promotes from `fail-likely` to `fail-known` and gets *skipped on future sweeps* (override with `--include-fail-known` to re-test post-fix).
- [x] **No manual testing in any spec** *(iter 82)*: 23 "# Manual testing" headers in projects.md → "# Self-verification" (sweep replace). Body-schema spec text reframed: agent runs smoke commands itself, eyeballs Behaviours coverage, records uncertainty in SUMMARY.md before declaring `<phase>DONE</phase>`. No human verification step. The rubric is the arbiter.
- [x] **Per-project post-mortem reflection** *(iter 86)*: created `reflection-template.md` with frontmatter (phase timings, self-confidence) + 7 body sections (goal-as-understood / approach / self-verification / deprecated / gave-up-on / **Dark CLI UX feedback** as the headline section / notes). Spec'd the wrapper aggregator (`python -m harness reflect <sweep_id>` → `cli-friction-digest.md`). Cross-language parity: TS/Py/Go/Rust runs fill the same template; aggregator can compare friction-counts across languages.

## P1 — doc-set restructure (readability)

The user explicitly said: *"no .md file should ever print to be more than like 10 pages long; break it up a bit."* + projects.md and improvements.md are too dense.

- [x] **`projects.md` → split per-project** *(iter 98)*: all 22 specs now in `ai-devloop/projects/` as standalone .md files (5 core projects + 3 Phase-1 + 6 library ports + 5 expected-fail + 3 breadth). Each spec is clean unwrapped markdown (frontmatter at top, `languages:` extended to `[dark, ts, py, go, rust]` where missing, agent-facing self-verification sections, closing core/cross-language meta-note). Follow-up: rewriting `projects.md` itself as a thin catalog/index pointing at the per-file specs is tracked as a separate P1 cleanup below.
- [x] **`projects.md` → catalog/index rewrite** *(iter 99)*: replaced the 1650-line inline-spec block (lines 190-1846) with a thin **§Per-project agent-facing specs** section: a 22-row index table grouped by subset (core projects / Phase-1 / library ports / expected-fail / breadth) linking to each `projects/<name>.md` standalone file, plus a cross-spec longitudinal-links list (e.g. csv → csv-to-json blocker retirement; redis-driver socket gap → 6-spec gap-closure event). File dropped from 2117 → 508 lines (76% reduction). All §Phase-2 / §Library-port-candidates / §Wider-catalog / §Cross-cutting / §Sources sections preserved verbatim.
- [x] **`improvements.md` → split per-recommendation** *(iter 105)*: all 29 §3.1-§3.6 recommendations now standalone files under `ai-devloop/improvements/`. Each file: frontmatter (title/section/priority/harness_signal) + Problem / Proposed fix / Harness signal sections + cross-refs. Each §3.x body in main file replaced with an N-row index table linking to the per-rec files. Round-2 P2 reframings (`dark-edit` AST-aware redesign, `tty-detection` JSON-vs-plaintext rule, `json-rollout` per-command audit) absorbed in-place into their respective standalone files. **improvements.md: 269 → 165 lines (-39%)**, well under the 10-page cap. Appendix sections (cross-cutting / strengths / doc-bugs / known-runtime-gaps) left in the main file as natural meta-commentary, not standalone recs.
- [x] **`plan.md` → separate plan from analysis** *(iters 106-107)*: created `plan-analysis.md`; migrated all 4 analysis-flavoured top-level sections — §2 (the thesis) + §2.1 (competitive landscape) + §5 (Open questions) + §8 (Risk / failure modes) + §References. Each replaced with a 1-2 line pointer in plan.md. **plan.md: 1574 → 1511 lines**; **plan-analysis.md: 86 lines**. Remaining length in plan.md is genuine spec content (harness layout, metrics, phasing, orchestration) — the load-bearing operational doc the user can skim. Subsection-level analysis-extraction within §4.x/§6.x is left for the "Cap remaining files at ~10 printed pages" follow-up if needed.
- [x] **Move dashboard-mock.html + future report samples into `ai-devloop/samples/`** *(iter 108)*: created `samples/`; moved `dashboard-mock.html` → `samples/dashboard-mock.html`. README.md links updated.
- [x] **Compact research-log.md** *(iter 109)*: archived original 1009-line log to `samples/historical/research-log-loops-1-3.md`; replaced `research-log.md` with a 28-line "retired" stub that explains where each kind of iter-finding now lives (feedback-plan.md / improvements/ / projects/ / plan-analysis.md / reflection-template.md). Picked option (c) per the in-bullet recommendation.
- [x] **PROGRESS.md retire** *(iter 108)*: created `samples/historical/`; moved `PROGRESS.md` → `samples/historical/PROGRESS.md`. README.md table row repathed.
- [x] **Cap any remaining file at ~10 printed pages** *(iters 110-118)*: every live `*.md` file now ≤508 lines (~10 pages).
  - **Iter 110**: §4.7 (Agent task prompt template, 190 lines) → `prompt-template.md`.
  - **Iter 111**: §4.10 (171 lines) → `launch-checklist.md`. §6.2 (179 lines) → `dashboard-spec.md`.
  - **Iter 118**: §4.8 Multi orchestration (178 lines) → `multi-orchestration.md`. §4.9 Nightly cadence (171 lines) → `nightly-cadence.md`. §7 Phasing (192 lines) → `phasing.md`.
  - **plan.md size trajectory**: 1574 → 1511 → 1321 → 1154 → 979 → 805 → 638 → **450 lines (~9 pages, under cap)**. 3.5× reduction overall.
  - **Spinoff files**: 7 standalone discrete-artifact files (plan-analysis, prompt-template, multi-orchestration, nightly-cadence, launch-checklist, dashboard-spec, phasing) — each is ≤245 lines. README index updated.

## P1 — process changes

- [x] **Reframe specs around agent self-verification** *(iter 82 sweep + iter 112 audit)*: 23 "# Manual testing" → "# Self-verification" headers swept iter 82. Iter-112 grep audit confirms zero residuals across `projects/` + `improvements/` + `*.md` (excluding archived samples/historical and the historical record in feedback-plan.md/research-log.md). README.md "Manual testing" mentions also fixed iter 112.
- [x] **Spec the queue runner concretely** *(iter 89, verified iter 114)*: `queue-mechanism.md` (224 lines) covers all 4 requirements — stateful process (queue-state.json + .lock), SIGTERM-resumable (wrapper crash + reboot tested), per-language subprocess sandbox (`bench-<sweep>-<project>-<lang>-<attempt>` ephemeral branches for Dark + `artifact-<lang>/` for TS/Py/Go/Rust), continuous `queue-state.json` emission. Maps onto Multi's queue per §4.8.
- [x] **Reflection template** *(iter 86)*: created `reflection-template.md` with frontmatter (project / language / sweep_id / phase timings / self-confidence) + 7 body sections incl. "Dark CLI UX feedback" headline. Wrapper aggregator spec'd (`python -m harness reflect <sweep_id>` → `cli-friction-digest.md`). Cross-language parity for friction-count comparisons. *(Already-done item swept into [x] in iter 109.)*

## P2 — improvements rethink

- [x] **TTY/non-TTY auto-detection** *(iter 100)*: standalone file at `improvements/tty-detection.md` with full framing — non-TTY callers get terse plaintext (no banner, no ANSI), savings automatic, no agent prompt change. The round-2 cross-reference question (default plaintext vs JSON) is captured in the file's closing note pointing at `json-rollout.md`'s per-command audit.
- [x] **Reconsider `--json` as the default** *(iter 100)*: round-2 reconsideration absorbed into `improvements/json-rollout.md` closing section — decision rule is "default to JSON for parser-friendly cases (search, deps); default to dense plaintext for human-readable structured (tree, list)". Per-command audit deferred to wave-1 implementation when the rollout actually ships.
- [x] **`for-ai` doc churn fix** *(iter 114)*: created `improvements/for-ai-bundle.md` — standalone recommendation for a `docs for-ai-bundle` command that emits everything an agent needs (workflow + syntax + common commands + package-tree summary + branch state + reference list) in a single dense call, replacing 3-5 turn-by-turn lookups. Added as row #7 in §3.1 Discovery index. Cross-refs `claude-md-template.md` for the autoload case + the doc-bug catalog for ambiguous-dispatch issues.
- [x] **Rethink "dark edit --replace"** *(iter 102)*: AST-aware redesign annotation absorbed into `improvements/dark-edit.md` closing section. Three concrete API shapes documented (`--rename-binding`, `--replace-call`, `--map-leaves`); the original text-replace framing is explicitly flagged as "wrong shape for Dark." Original §3.2 #1 stub kept in the index table for continuity but the standalone file is the canonical proposal.
- [x] **CLI daemon trade-off spec** *(iter 115)*: created `improvements/cli-daemon.md` — a 6-axis "for" table (cold-start amortization, package-tree cache, persistent watcher, faster autocomplete, telemetry correlation, streaming stdin) and 6-axis "against" table (lifecycle complexity, signal handling, multi-user contention, isolation, observability, debuggability). **Recommendation**: Phase 4+ as opt-in (`dark --daemon`); Phase 1-3 keep measuring cold-start; meantime reduce orient-call count via claude-md-template + for-ai-bundle. Added as row #6 in §3.2 index.

## P2 — bench scope deepening

- [x] **Add Go and Rust spec language-pinning** *(iter 81)*: plan.md §4.3.1 framework-pinning policy now lists `golang:1.23-alpine` (with quarterly bump to 1.24) and `rust:1.83-slim-bookworm` (similar quarterly cadence). Verified iter 113 — both lines present at plan.md lines 190-191.
- [x] **Pricing.json for 5 languages** *(iter 116)*: spec'd in `dashboard-spec.md` "Cost attribution" subsection — pricing.json keys stay language-agnostic (per-model), runs join to cost via the `language: <dark|ts|py|go|rust>` field already in `runs.jsonl`. Cost-attribution formula spec'd inline. No `pricing.json` change required.
- [x] **Cross-language results don't aggregate across cadences** *(iter 116)*: spec'd in `dashboard-spec.md` "Cross-cadence display" subsection — 5 explicit display rules (each cell shows sweep_id timestamp; Dark column = latest Dark; other-language columns = latest baseline of each, not synced; over-time view plots each language as separate line with step function for sparse baselines; what-changed view is Dark-only by construction; cost-cap header is tonight's-config not per-cadence). Banner annotation: "Dark vs others uses the latest baseline of each. Click for sweep_ids."

## P2 — quality

- [x] **Audit the spec catalog after the docs split** *(iter 112)*: grep across the live doc set for "Manual testing" / "human verifies" / "human can verify" / "the human verifies" returned only 2 hits, both in README.md (now fixed). All `projects/<spec>.md` files were already iter-82-compliant; no per-spec edits needed. The audit closes a check-of-a-check on iter-82's sweep.
- [x] **Update README** after each major restructure so it accurately reflects the file layout *(iter 108)*: README.md file table fully revised — new rows for plan-analysis.md / improvements/ dir / projects/ dir / feedback-plan.md, line counts refreshed, samples/ paths corrected. Becomes a recurring discipline going forward (re-touch after any future restructure).
- [x] **Don't add to research-log.md going forward** *(iter 81 onward)*: in effect since loop-3 kicked off; explicit in `ai-devloop-prompt.md`. Iter-109 retirement of the file (above) makes this physically enforced rather than discipline-only.

---

## New round-2 feedback received mid-loop

- [x] **Make the real dashboard denser** *(iter 117)*: revised `samples/dashboard-mock.html` with a tighter layout — smaller font (13.5px), shorter line-height (1.35), 4-column headline-metrics stat grid (with sparklines), 2-column "what changed + workarounds/self-health" block, per-tier pass-rate table now shows all 5 languages inline (replacing a stacked-bar chart placeholder), max-width raised 1100→1280px, padding reduced throughout. The mock is now ~3 screens dense vs ~5 sparse. Implementer's visual target is set.

## Rolling status

- **Iter 81** (2026-05-05 mid-day): kickoff of round-2 loop. Closed 2 of P0's 11 items: cross-language scope expanded to 5 langs (Dark/TS/Py/Go/Rust); cadence re-framed (weekly cross-lang + every-day-or-two Dark, both manually triggered). Captured 1 new round-2 item (denser dashboard). Net feedback-plan progress: **2 / 25** items checked.
- **Iter 82**: closed P0 #10 (no manual testing) — swept 23 "# Manual testing" → "# Self-verification" in projects.md; body-schema reframed for agent self-check. Net progress: **3 / 25** items checked.
- **Iter 83**: closed P0 #6 + #7 in one tick — created `tonights-queue.md` (the 10 projects, priority order, spread analysis) + `blockers.md` (4 real blockers with severity + 6-step pre-launch checklist). Net progress: **5 / 25** items checked.
- **Iter 84**: closed P0 #5 — no `dark/` workspace dir; Dark scopes to ephemeral `bench-<sweep_id>-<project>-<run_id>` branches; TS/Py/Go/Rust workspace dirs purged before each new impl, no peeking. §4.0 + §4.8 + §4.10 updated. Net progress: **6 / 25** items checked.
- **Iter 85**: closed P0 #3 — no cost cap tonight; pace by concurrency only. §4.9 "Tonight's-kickoff override" subsection. Subsequent runs use 2× tonight's medians as soft cap. Net progress: **7 / 25** items checked.
- **Iter 86**: closed P0 #11 — created `reflection-template.md`. Frontmatter + 7 sections incl. "Dark CLI UX feedback" headline. Wrapper aggregator spec'd (`python -m harness reflect`). Cross-language parity for friction-count comparisons. Net progress: **8 / 25** items checked.
- **Iter 87**: closed P0 #9 — §4.9 cut-off policy: 50 turns OR 15 min wall (whichever first). Records partial SUMMARY.md + last 100 transcript lines. **3-consecutive-cutoff rule auto-promotes `fail-likely` → `fail-known` (skipped on subsequent sweeps).** Net progress: **9 / 25** items checked.
- **Iter 88**: closed P0 #8 — new `class: interactive-cli` with tmux-send-keys testing pattern. Spec format extended (new "Keystroke script" body section). 4 candidates listed; spec materialization deferred. Net progress: **10 / 25** items checked.
- **Iter 89**: closed P0 #4 — created `queue-mechanism.md`. 8-phase lifecycle, queue-state.json + .lock for resumability, scheduler concurrency=4, CLI surface (sweep/status/cancel/reflect/report/dashboard). **All P0 items now closed.** Net progress: **11 / 25** items checked.
- **Iter 90**: started P1 — projects.md per-project split (multi-iter task). Created `ai-devloop/projects/` dir; extracted 2 of 22 core specs (password-gen, cron-describe) as clean unwrapped .md files. 20 specs to go. Net progress: **11 / 25** + 2 sub-progress.
- **Iter 91**: continued projects.md split. Extracted 3 more core specs (markdown-toc, validation-applicative, parser-combinators) — **all 5 core projects now in `projects/`**. 17 specs remaining (library ports + Phase-1 picks + expected-fail + breadth). Net progress: **11 / 25** + 5 sub-progress.
- **Iter 92**: continued projects.md split. Extracted all 3 Phase-1 specs (hello-cli, csv-to-json, url-shortener-cli) — **the entire tonight's-queue Phase-1 + core set is now in `projects/`** (8 of 10 tonight's-queue items have standalone files). 14 specs remaining (6 library ports + 5 expected-fail + 3 breadth). Net progress: **11 / 25** + 8 sub-progress.
- **Iter 93**: continued projects.md split. Extracted 2 library-port specs (mvu-runtime, pretty-printer) — both ethos/internal-user library ports per iter-29/iter-58. mvu-runtime is the 9th of 10 tonight's-queue items. 12 specs remaining (4 library ports + 5 expected-fail + 3 breadth). Net progress: **11 / 25** + 10 sub-progress.
- **Iter 94**: continued projects.md split. Extracted 2 more library-port specs (url-builder-parser, json-pointer) — Elm `Url` port + RFC 6901 JSON Pointer. 10 specs remaining (2 library ports + 5 expected-fail + 3 breadth). Net progress: **11 / 25** + 12 sub-progress.
- **Iter 95**: continued projects.md split. Extracted last 2 library-port specs (pcg-random, csv) — **all 6 library-port specs now in `projects/`**. 8 specs remaining (5 expected-fail + 3 breadth). Net progress: **11 / 25** + 14 sub-progress.
- **Iter 96**: continued projects.md split. Extracted 2 expected-fail specs (parallel-downloader → no-async-primitives, jwt-rs256 → no-rsa-signing). 6 specs remaining (3 expected-fail + 3 breadth). Net progress: **11 / 25** + 16 sub-progress.
- **Iter 97**: continued projects.md split. Extracted 3 expected-fail specs (tar-zip-creation → no-archive-creation, realtime-roguelike → no-non-blocking-stdin, redis-driver → no-raw-sockets) — **all 5 expected-fail specs now in `projects/`**. 3 breadth picks remaining. Net progress: **11 / 25** + 19 sub-progress.
- **Iter 98**: closed P1 #1 — extracted last 3 breadth-pick specs (cron-lite daemon, mcp-fs MCP server, pr-titler LLM-CLI). **All 22 per-project specs now standalone files in `ai-devloop/projects/`**. Item marked `[x]`; spawned a follow-up P1 catalog/index rewrite of projects.md itself (~2100 lines → <300). Net progress: **12 / 26** items checked.
- **Iter 99**: closed the iter-98-spawned follow-up — replaced the 1650-line inline-spec block in projects.md with a 22-row per-spec index table + cross-spec longitudinal-links list. **projects.md: 2117 → 508 lines (76% reduction)**, well under the user's 10-printed-page cap. Net progress: **13 / 26** items checked.
- **Iter 100-101**: started P1 #2 — improvements.md split per-recommendation. Created `improvements/` dir; extracted all 6 §3.1 Discovery items (claude-md-template / search-ranking / did-you-mean / dark-suggest / tty-detection / json-rollout) as standalone files; replaced §3.1's body in main file with a 6-row index table. **improvements.md: 269 → 225 lines** so far (more reduction comes as §3.2-§3.6 are split). Net progress: **13 / 26** + 6 sub-progress on improvements split.
- **Iter 102**: continued §3.2 Authoring split. Extracted 5 standalone files (dark-edit, auto-emit-diagnostics, fn-update-required, parse-error-suggestions, sticky-branch); §3.2 body replaced with a 5-row index table. **dark-edit.md** absorbs the round-2 P2 "AST-aware redesign" annotation. **improvements.md: 225 → 207 lines**. Net progress: **13 / 26** + 11 sub-progress on improvements split.
- **Iter 103**: continued §3.3 Verification split. Extracted 5 standalone files (auto-attach-trace, run-replay-shorthand, gen-test-promotion, hotspots-review, traces-view-json); §3.3 body replaced with a 5-row index table. **improvements.md: 207 → 209 lines** (index table marginally bigger than the prior 5-bullet list; the real gain is the 5 standalone files now individually loadable/printable). Net progress: **13 / 26** + 16 sub-progress on improvements split.
- **Iter 104**: continued split — §3.4 Iteration (4 files: dark-rename, dark-uncommit-revert, session-changed-view, merge-rebase-prompt-surfacing) + §3.5 Sharing (4 files: dark-publish, dark-publish-wasm, reproducible-builds, dark-export-import). Both section bodies replaced with index tables; cross-refs collapsed. **improvements.md: 209 → 185 lines** (-24 lines). Net progress: **13 / 26** + 24 sub-progress on improvements split.
- **Iter 105**: closed P1 #2 — finished §3.6 Review split (5 files: dark-review, dark-show-json, agent-summary, deprecated-audit, review-mark). All 29 recommendations now standalone files under `improvements/`. **improvements.md: 185 → 165 lines (-39% from 269 baseline)**, well under 10-page cap. Net progress: **14 / 26** items checked.
- **Iter 106**: started P1 #3 — plan.md spec-vs-analysis split. Created `plan-analysis.md`; migrated §2 (the thesis) + §2.1 (competitive landscape) — 42 lines of pure rationale. Replaced §2 in plan.md with a 4-line pointer. **plan.md: 1574 → 1540 lines** so far; further analysis sections (§5 / §8 / §References) queued for iter 107. Net progress: **14 / 26** + 1 sub-progress on plan split.
- **Iter 107**: closed P1 #3 — migrated §5 Open questions + §8 Risk / failure modes + §References to plan-analysis.md; replaced each with 1-2 line stub pointers. **plan.md: 1540 → 1511; plan-analysis.md: 52 → 86**. All major analysis sections now live in plan-analysis.md; plan.md is the load-bearing spec the user skims. Net progress: **15 / 26** items checked.
- **Iter 108**: knocked out two small file-move items in one tick — created `samples/` + `samples/historical/`; moved `dashboard-mock.html` → `samples/`; moved `PROGRESS.md` → `samples/historical/`. README.md table revised: dashboard-mock.html link repathed, PROGRESS.md row repathed, **new rows added for plan-analysis.md / improvements/ dir / projects/ dir / feedback-plan.md**, plus all line counts updated to current. Top-level dir now cleaner. Net progress: **17 / 26** items checked.
- **Iter 109**: closed P1 #4 (compact research-log.md) + swept 3 already-done items: P1 #5 (reflection template, done iter 86), P2 #21 (Update README, done iter 108), P2 #22 (Don't add to research-log going forward, in effect since iter 81). research-log.md: 1009 lines archived to `samples/historical/research-log-loops-1-3.md`; replaced with a 28-line "retired" stub redirecting readers to the load-bearing docs. README updated again. Net progress: **21 / 26** items checked.
- **Iter 110**: started P1 #6 (cap files at ~10 printed pages). Spot-checked all .md files; only `plan.md` is over (1511 lines ~26 pages). Extracted §4.7 (Agent task prompt template, 190 lines) → standalone `prompt-template.md` — discrete artifact (the literal template files the wrapper sends to the agent), reproducibility-critical via `prompt_template_hash`. **plan.md: 1511 → 1321 lines**. Item left `[ ]` because plan.md still over cap; sub-bullets queue further §4.10/§6.2/§4.8/§4.9 extracts. README updated with new row. Net progress: **21 / 26** + 1 sub-progress on cap item.
- **Iter 111**: continued cap work — extracted §4.10 (Tonight's launch checklist, 171 lines) → `launch-checklist.md` and §6.2 (Dashboard + exportable reports, 179 lines) → `dashboard-spec.md`. **plan.md: 1321 → 979 lines** (~19 printed pages, still over the 10-page cap but down from ~30 originally). README rows added for both new files. Item still `[ ]` because plan.md remains over cap; further extraction requires moving §4.8 / §4.9 / §6.0 — those are debatable as standalone-file candidates, deferred unless user signals 10-page cap is hard.
- **Iter 112**: closed two related items in one tick — P1 #7 (Reframe specs around agent self-verification) and P2 #20 (Audit the spec catalog for residual references). grep audit across the live doc set found 2 stale "Manual testing" mentions in README.md (now fixed); all 22 per-spec files in `projects/` are iter-82-compliant. Net progress: **23 / 26** items checked.
- **Iter 113**: swept 4 already-done P2 items into `[x]` — TTY/non-TTY (absorbed into `improvements/tty-detection.md` iter 100), `--json` reconsider (absorbed into `improvements/json-rollout.md` iter 100), AST-aware dark-edit redesign (absorbed into `improvements/dark-edit.md` iter 102), Go/Rust pinning (added to plan.md §4.3.1 iter 81 — verified at lines 190-191). Each item's per-rec / spec home is the canonical proposal; the feedback-plan stub is now a redirect. **Recount of feedback-plan items**: 27 checked / 7 unchecked / 34 total (the original "25" / "26" rolling counts were stale — the spawned follow-ups + sub-bullet items are counted now). Net progress: **27 / 34**.
- **Iter 114**: closed two items — P1 #8 (Spec the queue runner concretely; verified `queue-mechanism.md` covers all 4 requirements) and P2 #13 (`for-ai` doc churn fix; created `improvements/for-ai-bundle.md` and added it as §3.1 row #7). improvements/ dir now has 30 standalone files. Net progress: **29 / 34**.
- **Iter 115**: closed P2 #15 — created `improvements/cli-daemon.md` (the user's "what problems would a CLI daemon help? along what axes?" question answered with a 6-axis pro/con analysis + Phase 4+ opt-in recommendation). Added as row #6 in §3.2 index (the §3.2 row count now correctly says "Five concrete items + 1 architectural analysis"). Net progress: **30 / 34**.
- **Iter 116**: closed P2 #17 + P2 #18 in one tick — added "Multi-language cost attribution + cross-cadence display" section to `dashboard-spec.md`. #17: pricing.json stays per-model (unchanged), runs join to cost via language tag — formula spec'd inline. #18: 5 display rules so weekly other-language baselines + every-2-day Dark runs render coherently (per-cell sweep_id timestamps, "latest of each" rule, step-function over-time plot, Dark-only what-changed view). Net progress: **32 / 34**.
- **Iter 117**: closed round-2 #25 (Make real dashboard denser) — rewrote `samples/dashboard-mock.html` with stat-block grids + sparklines, 2-column blocks, per-tier table showing all 5 languages inline, smaller font/line-height/padding throughout. Replaced 4 chart-placeholder boxes with inline stat-block + spark visualizations. Implementer's visual target is now denser. Net progress: **33 / 34**.
- **Iter 118**: closed P1 #6 (Cap files at ~10 printed pages). Three more extracts from plan.md — §4.8 (178 lines) → `multi-orchestration.md`, §4.9 (171 lines) → `nightly-cadence.md`, §7 (192 lines) → `phasing.md`. **plan.md: 979 → 450 lines (~9 pages, finally under the 10-page cap)**. README index updated; 7 spinoff files now back the plan.md spec. **All 34 feedback-plan items are checked.** The round-2 loop is complete.

## Stop condition

When all `[ ]` here are checked or explicitly deferred-with-rationale, stop the loop. The user is back in "a few hours" and will provide round 3.

If a tick reveals something genuinely blocked on user input (e.g. "what's the actual cost budget for tonight?"), park the item with a `[ ] (blocked: reason)` annotation rather than a checked box.

---

## How the loop should treat this file

- Each iteration: open this file, pick the next `[ ]` (highest-priority unchecked), do the work, replace `[ ]` with `[x]` + a one-line note about what landed.
- If a single item is too big for one iter (e.g. "split projects.md per-project"), do it across multiple iters and keep `[ ]` until fully done; add sub-bullets as progress markers.
- The loop's existing variety rule (concretize / decide / verify / etc.) still applies; spec-restructure is concretize, the AST-edit rethink is decide, etc.
- Every Nth iter: write a short status line at the bottom of this file ("Progress as of iter N: M of N items checked") so the user can scan it on return.
