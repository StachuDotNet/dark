# pdd-thinking/archive — historical artifacts

What remains after the consolidation pass: the elevator pitches
(tightened — kept just the 60-sec and 3-min versions, dropped the
deep-dive duplication of CLAIMS/ALGORITHM and the spike-era "what
I'm not claiming" framing).

Everything else (session reports, early planning docs, the
spike-end reflection layer, the SCM-INTEGRATION pivot sketch, the
FINAL-REPORT) was absorbed into the top-level docs (CLAIMS,
ALGORITHM, FRONTIER, WRAP-UP) and deleted. `git log` is the
record for anyone tracing provenance.
